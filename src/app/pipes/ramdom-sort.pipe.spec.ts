import { RamdomSortPipe } from './ramdom-sort.pipe';

describe('RamdomSortPipe', () => {
  it('create an instance', () => {
    const pipe = new RamdomSortPipe();
    expect(pipe).toBeTruthy();
  });
});
