import { ShowResultDirective } from './show-result.directive';

describe('ShowResultDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowResultDirective();
    expect(directive).toBeTruthy();
  });
});
